 //21. Write a C program to accept two integers and check whether they are equal or not

#include <stdio.h>

void main()
{
    int x, y;

    printf("Enter the values: ");
        scanf("%d %d", &x, &y);
    if (x == y){

        printf("num1 and num2 are equal\n");
    }
        else{

        printf("num1 and num2 are not equal\n");
        }

    
}
